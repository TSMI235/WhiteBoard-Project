<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">    
    <title>Whiteboard</title>
    <style>
        body {
            margin: 0;
            padding: 0;
        }
        #info{
            z-index:10;
            position: absolute;
        }
        #thecanvas{
            z-index:1;
            position: absolute;
            border: 2px solid black;
        }
        .hidden{
            display: none;
        }
    </style>
    <meta name="turbolinks-cache-control" content="no-preview">
    <script src="//ajax.googleapis.com/ajax/libs/jquery/1.9.1/jquery.min.js"></script>
    <%= javascript_include_tag "board", "data-turbolinks-track" => true %>
    <img src ="https://s3.amazonaws.com/whiteboard-csc3380/<%= @whiteboard.id%>?<%= rand(0..1000) %>" crossorigin="anonymous" class = "hidden" id = "savedImage">

  </head>

  <body>
        <p hidden id=hash-id><%= @whiteboard[:hash_id]%></p>
        <p hidden id=id><%= @whiteboard[:id]%></p>
        <div id=info>
			<nav class="navbar navbar-inverse navbar-fixed-top">
				<div class="container-fluid">
					<div class="navbar-header">
						<a class="navbar-brand">WhiteBoard</a>
					</div>
					<ul class="nav navbar-nav">
						<li class="active"><a href="../home/index">Home</a></li>
						<li><a href="../whiteboards">My Whiteboards</a></li>
						<div class="btn-group" id=info>
							<button type="button" class="btn btn-inverse navbar-btn role="button"><%= link_to "Edit", edit_whiteboard_path(@whiteboard) %></button>
							<button type="button" class="btn btn-inverse navbar-btn role="button"><%= link_to "Delete", @whiteboard, method: :delete, data: { confirm: "Are you sure you want to delete this whiteboard?"} %></button>
							<button type="button" class="btn btn-inverse navbar-btn role="button" id='save'>Save</button>
						</div>
					</ul>
					<ul class="nav navbar-nav navbar-right">
						<% if current_user %>
							<li><a href=<%= edit_user_registration_path %>><span class="glyphicon glyphicon-user"></span><%= @username %></a></li>
							<li><a class="glyphicon" rel="nofollow" data-method="delete" href="/users/sign_out">Sign Out</a></li>
						<% else %>
							<li><a href=<%= new_user_registration_path %>><span class="glyphicon glyphicon-user"></span> Sign Up</a></li>
							<li><a href=<%= new_user_session_path %>><span class="glyphicon glyphicon-log-in"></span> Login</a></li>
						<% end  %>
					</ul>
				</div>
			</nav>
			<p style="margin-top:65px">
			<button id="drawmode" style="background-color: green;">DRAW MODE</button>
			<select ID = "Color">
				<option value="blue">Blue</option>
				<option value="red">Red</option>
				<option value="green">Green</option>
				<option value="Chocolate">Brown</option>
				<option value="purple">Purple</option>
				<option value="black">Black</option>
			</select>
			<p ID="tag">Size: 5</p>
			<p>Press 1 for Marker, Press 2 for Highligther, Press 3 for Eraser</p>
			<p></p>
			<h3><%= @title %></h3>
        </div>
        <div id=thecanvas>
        <canvas id="board" width=<%= @width %> height=<%= @height %>></canvas>
        </div>

  </body>
  <script>
        var hashid = $('#hash-id')[0].innerHTML;
        var id = $('#id')[0].innerHTML;
    </script>
</html>